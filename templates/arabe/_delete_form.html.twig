<form method="post" action="{{ path('app_arabe_delete', {'id': arabe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ arabe.id) }}">
    <button class="btn">Delete</button>
</form>
