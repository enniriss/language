<form method="post" action="{{ path('app_japonais_delete', {'id': japonai.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ japonai.id) }}">
    <button class="btn">Delete</button>
</form>
