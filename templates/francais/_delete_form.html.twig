<form method="post" action="{{ path('app_francais_delete', {'id': francai.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ francai.id) }}">
    <button class="btn">Delete</button>
</form>
